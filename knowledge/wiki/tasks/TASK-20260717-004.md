---
id: TASK-20260717-004
title: "Clean and reproduce the ESP32-C6 GEO IMU fusion benchmark"
status: active
created: 2026-07-17
updated: 2026-07-17
task_type: validation
confidence: documented
related_entities:
  - component:esp32-imu-fusion-benchmark
  - experiment:esp32-imu-fusion-20260717
related_decisions:
  - decision:sparse-fusion-before-backend-emission
---

# TASK-20260717-004 — Clean and reproduce the ESP32-C6 GEO IMU fusion benchmark

## Objective

Move the validated benchmark from stacked development history onto a minimal branch based on current `main`, then reproduce the build and startup behavior without importing unrelated v0.19 work.

## Scope

- standalone ESP-IDF project;
- generic and fused GEO float/Q16 paths;
- conventional quaternion and TinyML fixtures;
- deterministic replay and common metric contract;
- startup equivalence checks;
- evidence capture and result summarization;
- dedicated compile workflow;
- benchmark-specific knowledge records.

## Completed

- Created `benchmark/esp32-imu-clean-v1` from `main`.
- Transplanted only 22 benchmark-related files.
- Removed dependency on the large development ESP32 component by compiling only `src/cl20.c` and `src/fixed.c` into the benchmark component.
- Opened clean draft PR #16.
- Preserved the predecessor physical result and exact evidence identity.
- Added local-artifact ignore rules.
- Recorded the component, experiment, and task in the knowledge system.

## Active gate

Build and flash the clean branch with ESP-IDF 5.3.4. The startup record must report:

```text
GEO_AB_FUSION_CHECKS,status=pass
```

A full reproduction should produce 181 CSV lines and `VALIDATION: PASS`.

## Acceptance criteria

- clean branch build succeeds;
- no source files outside the benchmark, `.gitignore`, and dedicated workflow are changed;
- all six implementations complete 30 runs;
- no deadline misses or NaNs;
- one stable hash per implementation;
- A1/C output hashes remain equal;
- aggregate results remain within expected run-to-run variation of the predecessor evidence;
- evidence directory records exact clean-branch commit and clean tracked status.

## Next task

After reproduction, begin automatic sparse schedule generation:

```text
geometric expression
→ grade/sparsity analysis
→ zero elimination
→ fused accumulation
→ fixed-point range analysis
→ deterministic C emission
```

## Unresolved questions

- Does the standalone main-based ESP-IDF project reproduce the predecessor hashes exactly?
- What cycle-level A1/C ratio is observed under randomized implementation order?
- Can generated A1/B1 schedules match the manually frozen output hashes and timing within 1%?
