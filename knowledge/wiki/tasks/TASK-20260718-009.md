---
id: TASK-20260718-009
title: "Add grammar-bounded exact identity discovery"
status: active
created: 2026-07-18
updated: 2026-07-18
task_type: research-implementation
confidence: implemented-unvalidated-on-gpu
related_entities:
  - component:geometric-identity-engine
  - experiment:geometric-identity-discovery-20260717
related_decisions:
  - decision:exact-modular-search-before-symbolic-proof
  - decision:canonical-polynomial-before-sampling
---

# TASK-20260718-009 — Add grammar-bounded exact identity discovery

## Objective

Move from testing and mutating supplied identities to generating bounded families of geometric expressions, classifying them by exact blade-wise integer polynomials, and selecting exact relations for independent host/CUDA validation.

## Implemented

- checked JSON grammar configuration;
- deterministic expression enumeration by AST cost;
- canonical syntax keys and commutative-addition normalization;
- exact blade-wise integer polynomial classification for every expression;
- exact polynomial equivalence classes;
- primitive polynomial classes modulo integer content and overall sign;
- integer-multiple relation recovery;
- deterministic scoring and bounded relation selection;
- exact structural certificates;
- automatic near-miss controls with nonzero polynomial differences;
- deterministic finite-field prechecks for controls;
- v1-compatible prime-matrix corpus emission;
- manifest compilation into the existing host/CUDA evaluator;
- host/CUDA result validation and witness reduction;
- reproducible evidence manifests and optional archives;
- deterministic unit tests and a host workflow;
- a Windows CUDA evidence runner.

## Initial grammar corpora

1. Vector products in `Cl(4,0)`.
2. General multivector reversion/product relations in signature `(2,2)`.
3. Nested commutator and Jacobi relations in `Cl(3,0)`.

Representative baseline relations include:

- `[a,b] = 2(a ∧ b)` for vectors;
- `reverse(a b) = reverse(b) reverse(a)`;
- `[a,b] = -[b,a]`;
- the cyclic commutator Jacobi expression equals zero.

These are controls for the discovery mechanism rather than claims of new results.

## Acceptance gate

- grammar parsing rejects invalid dimensions, signatures, variables, and operators;
- enumeration is byte-for-byte deterministic;
- exact polynomial classification agrees with the v2 backend;
- scalar-multiple classes recover correct integer multipliers;
- selected relations reclassify to a zero difference polynomial;
- generated finite-field specifications load in the v1 compiler;
- near-miss controls have nonzero polynomials and exact precheck witnesses;
- generated corpora are deterministic across repeated builds;
- host-generated evaluators pass;
- CUDA identity rows return no witness;
- CUDA near-miss rows produce host-reproducible exact witnesses;
- reduced witnesses remain valid;
- evidence validation and finalization pass.

## Current boundary

The grammar front end, three initial grammars, exact relation selector, tests, host workflow, and CUDA evidence runner are implemented. Hosted GitHub Actions attempts currently terminate before executing any job steps, so local exact-host and physical CUDA validation remain active gates.

## Physical smoke matrix

```powershell
& .\benchmarks\geo_identity_search\scripts\run_identity_grammar_discovery.ps1 `
    -Assignments 131072 `
    -CpuChecks 512 `
    -Primes @(65521,65519) `
    -PrecheckAssignments 1024 `
    -MaxRelations 8 `
    -MaxControls 2 `
    -Archive
```

## Full physical matrix

```powershell
& .\benchmarks\geo_identity_search\scripts\run_identity_grammar_discovery.ps1 `
    -Assignments 262144 `
    -CpuChecks 1024 `
    -Primes @(65521,65519,65497,32749) `
    -PrecheckAssignments 2048 `
    -MaxRelations 12 `
    -MaxControls 4 `
    -Archive
```

## Next stages

1. Physically validate the smoke and full matrices.
2. Add canonical variable-renaming and permutation quotienting.
3. Add dimension and signature sweeps for each grammar.
4. Rank relations by novelty against a checked identity library.
5. Seed characteristic-zero integer witnesses from reduced modular controls.
6. Export exact zero-polynomial certificates to proof-assistant formats.
7. Extend the grammar to contraction, duality, and additional involutions.
8. Apply the engine to representation, Lie-algebra, topology, and operator-family searches.
