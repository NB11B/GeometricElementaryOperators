---
id: TASK-20260716-001
title: "Complete v0.18.2 release hardening and validate exact candidate"
status: active
created: 2026-07-16
updated: 2026-07-16
task_type: implementation
confidence: verified
related_entities:
  - component:release-hardening-v0182
  - failure:github-actions-run-215-pre-checkout
related_decisions:
  - decision:v0182-hotfix-isolation-and-release-evidence
related_experiments: []
---

# TASK-20260716-001 — Complete v0.18.2 release hardening and validate exact candidate

## Original request

Take over building and testing the remaining kernel correctly, save verified work to GitHub, keep v0.18.2 isolated from v0.19/CUDA until hotfix proof.

## Objective

Close all v0.18.2 correctness, ABI, benchmark, EML, RTL, controller, and CI gates at PR #13 exact head, then independently review and publish the verified hotfix branch.

## Initial constraints

- Preserve unrelated working-tree changes.
- Keep CUDA and all v0.19 backend work out of the v0.18.2 hotfix.
- Preserve the legacy public ABI, including the caller-visible size and offsets of `geo_optimized_witness_t`.
- Treat an Actions run that fails before checkout as infrastructure evidence only, not as a code pass or failure.
- Do not merge or tag until GitHub CI executes against the exact pushed candidate and passes.

## Expected deliverables

- A compileable v0.18.2 hotfix candidate containing the ABI, benchmark, EML, RTL overflow, controller, and CI hardening already assembled on PR #13.
- Complete local release, sanitizer, Q-format, ESP32 component, report/schema, RTL simulation/synthesis, and Windows/MSVC validation.
- Independent review of the resulting diff and validation evidence.
- A pushed PR #13 candidate whose exact-head GitHub CI is allowed to execute before merge or tag.

## Unknowns

- The final pushed commit identifier is not yet available because root acceptance, commit, and push follow knowledge curation.
- GitHub Actions run #215 failed before checkout; whether a new exact-head run will start and expose normal job steps remains unresolved.

## Context retrieved

- The worktree is based on PR #13 head `94cb66e4f71e3da2ceba96e3558f2cd542fe1b19` on `release-hardening-v0182`.
- The checkpoint branch `checkpoint/release-hardening-v0182-20260717` preserves that PR head.
- `docs/v0.18.2-release-gate.md` defines the local and hosted release evidence and requires an exact candidate identity.
- GitHub Actions run #215 reported failures across all jobs with empty step lists and no usable logs, showing that repository checkout and code execution never began.
- No durable repository knowledge existed before this task; the knowledge store and this task record were initialized before implementation.

## Repository evidence

- `include/geo/optimizer.h` retains the legacy `geo_optimized_witness_t` layout and documents explicit terminal-count handoff to later passes.
- `include/geo/folding.h` and `src/folding.c` carry terminal count as an explicit folding argument rather than adding a field to the public structure.
- `tests/test_release_hardening.c` and `tests/test_abi_consumer.c` validate legacy layout, hardened behavior, and public consumer linking.
- `src/executor_api.c` supplies externally linked definitions for `geo_struct_program_execute` and `geo_banked_execute`.
- `benchmarks/bench_complete.c`, `src/eml_embedded.c`, and `tools/generate_rtl.py` contain the selected-path benchmark, finite logarithm checks, fixed-point overflow interface, controller protocol, and their generated tests.
- `.github/workflows/ci.yml` defines release, sanitizer benchmark, Q-format, Windows ABI, RTL simulation, and synthesis jobs with pipeline failure propagation.
- `tests/test_safety.c` still assigned the removed `terminal_count` structure member at the scanned PR head, contradicting the restored legacy layout and preventing compilation.

## Plan

1. Scan PR #13 at its exact head against every release-gate requirement and preserve v0.19/CUDA isolation.
2. Remove only the obsolete `optimized.terminal_count` assignment from the malformed-program safety test.
3. Run the complete local Unix-like and Windows validation matrix, including sanitizer execution of the selected-path benchmark and archive-symbol inspection.
4. Run an independent read-only review of the actual product diff and validation evidence.
5. Curate durable task, decision, failure, and graph records; regenerate and validate knowledge views.
6. After root acceptance, commit and push the hotfix candidate, then require exact pushed-head GitHub CI before merge or tag.

## Acceptance criteria

- GCC and Clang Release builds pass in float and double precision.
- Clang ASan/UBSan builds pass in float and double precision, including the selected-path benchmark under CTest.
- `geo_struct_program_execute` and `geo_banked_execute` are defined archive symbols.
- Windows/MSVC float and double consumers build and pass the ABI/link suite.
- Q1, Q8, Q16, Q24, and Q30 fixed-point tests pass.
- The ESP32 component builds.
- Benchmark CSV, JSON, and Markdown generation and schema validation pass with selected-path labeling.
- Generated fixed-point product and controller RTL pass nominal/overflow simulation and Yosys synthesis.
- Independent review reports no BLOCKER or HIGH finding.
- No CUDA or v0.19 change is introduced in the hotfix.
- The exact pushed head executes and passes GitHub CI before merge or tag.

## Implementation

The scanner found one exact-head compilation defect: `tests/test_safety.c` wrote `optimized.terminal_count`, although v0.18.2 deliberately restored the legacy `geo_optimized_witness_t` layout and moved terminal count to explicit API arguments. The executor removed only that obsolete assignment. No production code, ABI, CUDA, or v0.19 backend behavior was changed by this correction.

## Files changed

- `tests/test_safety.c`: removed one obsolete write to the non-existent `geo_optimized_witness_t.terminal_count` member.
- `knowledge/wiki/tasks/TASK-20260716-001.md`: recorded task provenance, plan, evidence, validation, review, and remaining gate.
- `knowledge/wiki/components/release-hardening-v0182.md`: summarized the reviewed hotfix surface and evidence.
- `knowledge/wiki/decisions/v0182-hotfix-isolation-and-release-evidence.md`: recorded isolation and exact-head merge policy.
- `knowledge/wiki/failures/github-actions-run-215-pre-checkout.md`: preserved the pre-checkout infrastructure failure.
- `knowledge/graph/nodes.jsonl` and `knowledge/graph/edges.jsonl`: added task, component, decision, failure, and validation relationships.
- `knowledge/generated/`: regenerated deterministic graph and unresolved-question views.

## Validation

| Command or gate | Result | Evidence |
|---|---|---|
| GCC Release, float and double | Passed, 25/25 tests in each precision | Local validation performed from this candidate using the commands in `docs/v0.18.2-release-gate.md` |
| Clang Release, float and double | Passed, 25/25 tests in each precision | Local validation performed from this candidate using the commands in `docs/v0.18.2-release-gate.md` |
| Clang ASan/UBSan, float and double | Passed, 25/25 tests in each precision, including `selected_path_benchmark` | Sanitizer configuration and CTest registration in `.github/workflows/ci.yml` and `CMakeLists.txt`; local run recorded by root |
| Archive ABI symbol inspection | Passed for both public executor symbols | `nm`/`llvm-nm` output inspected in local GCC, Clang, and Windows validation |
| Windows/MSVC, float and double | Passed, 24/24 tests in each precision | Local MSVC builds and CTest runs; LLVM archive-symbol inspection also passed |
| Q-format matrix Q1/Q8/Q16/Q24/Q30 | Passed | `tests/test_qformat.c` executed at each configured fractional width |
| ESP32 component configure/build | Passed | `ports/esp32/CMakeLists.txt` component gate |
| Benchmark report and schema | Passed | `tools/benchmark_report.py` and CMake report target |
| Icarus fixed-product simulation | Passed nominal and overflow cases | Generated product and testbench from `tools/generate_rtl.py` |
| Icarus controller simulation | Passed dependency, completion, and overflow behavior | Generated controller and testbench from `tools/generate_rtl.py` |
| Yosys product/controller synthesis | Passed | Both generated SystemVerilog top modules synthesized successfully |
| Independent product review | PASS | Reviewer found no BLOCKER or HIGH issue in the actual diff or evidence |
| GitHub Actions run #215 | Infrastructure failure before checkout; not a product result | Empty job step lists and unavailable logs recorded during PR inspection |

## Review

- Verdict: PASS.
- Blockers: None.
- High findings: None.
- Test-quality conclusion: the one-line fix aligns the malformed-program regression with the restored ABI, while the full local matrix exercises the release gates affected by PR #13.
- Remaining risk: a fresh exact pushed-head GitHub run still must execute and pass; run #215 supplied no repository-code evidence.

## Decisions made

- Keep v0.18.2 isolated from CUDA and v0.19 backend integration.
- Preserve `geo_optimized_witness_t` binary layout and pass terminal count explicitly to folding.
- Accept local cross-platform validation and independent review as candidate evidence, but not as authorization to merge or tag without exact pushed-head GitHub CI.
- Classify run #215 as an infrastructure startup failure because no checkout, steps, or repository code execution occurred.

## Knowledge graph changes

- Added `task:task-20260716-001` for this release-hardening task.
- Added `component:release-hardening-v0182` for the hotfix surface.
- Added `decision:v0182-hotfix-isolation-and-release-evidence` for branch isolation and exact-head gating.
- Added `failure:github-actions-run-215-pre-checkout` for the runner startup failure.
- Connected the task to the component, decision, failure, and validation evidence.

## Unresolved questions

- Will the next exact pushed-head GitHub Actions run start normally and pass every required job?
- What commit identifier will root acceptance publish as the final PR #13 candidate?

## Final status

The product change is implemented, locally validated across the full available matrix, and independently reviewed with verdict PASS. This task remains active until root commits and pushes the candidate and GitHub CI executes successfully against that exact head. PR #13 must remain draft, unmerged, and untagged until then.
